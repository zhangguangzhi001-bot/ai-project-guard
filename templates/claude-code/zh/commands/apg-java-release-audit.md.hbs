# /apg-java-release-audit

## 目的

以资深 Java 架构师、金融系统代码审计专家、上线前质量门禁负责人视角，对 {{projectName}} 或当前供应商 Java 系统源码进行上线前质量审计。

本命令不是普通代码 Review，而是判断系统是否存在影响上线的严重质量问题、生产事故风险、安全漏洞、数据一致性风险，以及金融业务中的金额、状态、权限、审计、幂等问题。

## 强制约束

- 只读源码，不允许修改任何代码、配置、模板或测试。
- 允许在用户指定或确认的审计报告目录中创建质量审计报告、对比报告、趋势分析报告；默认不要修改业务源码目录内容。
- 不要做泛泛而谈的建议；所有问题必须引用具体文件、类、方法、代码位置和关键代码片段。
- 不要为了找问题而找问题；只输出真实可能影响生产的风险。
- 对不确定的问题标记为“需要进一步验证”，不要直接定性为阻断。
- 优先审计高风险文件和模块，不要平均用力。
- 代码风格问题默认不列入 P0/P1，除非会造成生产风险。
- 如需运行 Maven/Gradle/SpotBugs 等命令，先说明命令会产生的本地构建产物，并获得用户确认；不要擅自执行会清理、生成或联网下载依赖的命令。
- 如发现 secret、token、password、access key、private key、签名 URL 或敏感业务数据，只引用字段名和文件位置，隐藏真实值。

## 输入参数

用户可在命令后指定轮次：

- `round1`：项目体检和风险地图，默认轮次。
- `round2`：高风险模块专项审计。
- `round3`：金融系统专项审计。
- `round4`：上线阻断判断。
- `round5`：供应商整改清单。
- `archive`：将本次审计结果按年月日归档为质量报告。
- `compare <日期A> <日期B>`：对比两个日期的质量报告。
- `trend <开始日期> <结束日期>`：分析指定时间段内的质量趋势。
- `full`：按顺序分阶段执行；代码量大时先完成 `round1`，不要急于下最终结论。

如果用户未指定轮次，先执行 `round1`。日期格式统一使用 `YYYY-MM-DD`。

## 审计范围和阅读顺序

优先阅读并分析：

1. `pom.xml` / `build.gradle` / `settings.gradle`
2. `application.yml` / `application.properties` / profile 配置
3. 启动类
4. Controller 层
5. Service 层
6. Mapper / Repository 层和 Mapper XML
7. 定时任务
8. 配置类
9. 安全认证、权限拦截器、Filter、Security 配置
10. 全局异常处理
11. 日志配置
12. 数据库访问、事务、缓存、消息队列、外部接口相关代码

当前 APG 标记的危险模块：{{inlineList risks.dangerousModules}}。

## 高风险文件和模块优先级

1. 全局异常处理
2. 权限拦截器 / Filter / Security 配置
3. Controller 入参校验
4. Service 事务边界
5. Mapper XML / SQL 拼接
6. 定时任务
7. 文件上传下载
8. 外部接口调用
9. Redis 分布式锁
10. 消息队列消费逻辑
11. 金额计算逻辑
12. 审批状态流转逻辑
13. 删除、撤回、作废、复核等关键操作
14. `application.yml` 配置
15. 日志配置

## 重点扫描项

重点检查以下问题是否真实存在生产风险：

- 空指针风险、异常吞掉、事务注解失效、大事务
- 并发问题、数据不一致、重复提交、幂等缺失
- 状态流转错误、缺少状态机约束
- 金额使用 `double` / `float`
- `BigDecimal` 构造、比较、精度、舍入方式错误
- SQL 拼接、SQL 注入、N+1 查询、慢 SQL 风险
- 外部接口无超时、无降级、无重试边界
- 文件上传风险、路径穿越、类型校验缺失
- 敏感信息泄露、日志泄露、配置泄露
- 权限绕过、越权查询、越权操作、数据权限只依赖前端
- 删除、作废、撤回、审批等关键操作缺少权限或状态校验
- 批处理、定时任务、消息消费重复执行风险
- 异常时部分成功、部分失败且不可恢复
- 生产问题不可定位、不可监控、不可恢复

## 严重等级定义

- P0：必须阻断上线，存在严重生产事故、安全事故、资金风险、数据错乱风险。
- P1：强烈建议上线前修复，可能导致稳定性、安全性或数据一致性问题。
- P2：可以上线后优化，但需要纳入整改计划。
- P3：普通代码规范问题。

## 报告归档与趋势分析

默认报告目录为 `.claude/audit-reports/java-release/`。如用户指定其他目录，以用户指定目录为准。

### 归档规则

- 本次审计报告保存到 `.claude/audit-reports/java-release/YYYY/MM/DD/`。
- 建议文件名：
  - `round1-risk-map.md`
  - `round2-high-risk-audit.md`
  - `round3-financial-audit.md`
  - `round4-release-gate.md`
  - `round5-vendor-remediation.md`
  - `summary.md`
- 如果同一天多次执行，先检查目标文件是否已存在；如已存在，询问用户是覆盖、追加版本号，还是仅输出到对话。
- 归档报告必须保留机器可对比的摘要指标：P0/P1/P2/P3 数量、阻断上线问题数、上线前必须修复数、需要进一步验证数、高风险模块数、已确认修复数、遗留问题数。
- 不要把真实 secret、token、password、access key、private key、签名 URL 或敏感业务数据写入报告。

### 摘要指标格式

每份可归档报告末尾增加：

```markdown
## 质量指标摘要

- 审计日期：YYYY-MM-DD
- 审计轮次：round1/round2/round3/round4/round5/full
- 审计范围：<模块/目录>
- P0 数量：0
- P1 数量：0
- P2 数量：0
- P3 数量：0
- 阻断上线问题数：0
- 上线前必须修复数：0
- 需要进一步验证数：0
- 高风险模块数：0
- 已确认修复数：0
- 遗留问题数：0
- 总体结论：<一句话>
```

### `compare <日期A> <日期B>`：质量报告对比

执行步骤：

1. 读取两个日期目录下的 `summary.md`；如不存在，读取该日期目录下所有 round 报告并汇总。
2. 对比 P0/P1/P2/P3 数量变化、阻断问题变化、上线前必须修复项变化、高风险模块变化、需要进一步验证项变化。
3. 列出新增问题、已消除问题、等级升高问题、等级降低问题、仍未解决问题。
4. 对比时必须基于问题标题、涉及文件、类、方法、代码位置进行匹配；不要只按标题匹配。
5. 输出是否质量改善、质量恶化或风险基本持平。

输出格式：

```markdown
## 质量报告对比：YYYY-MM-DD vs YYYY-MM-DD

### 一、总体变化

### 二、指标变化表

| 指标 | 日期A | 日期B | 变化 |
| --- | ---: | ---: | ---: |
| P0 数量 |  |  |  |
| P1 数量 |  |  |  |
| P2 数量 |  |  |  |
| P3 数量 |  |  |  |
| 阻断上线问题数 |  |  |  |
| 上线前必须修复数 |  |  |  |
| 需要进一步验证数 |  |  |  |

### 三、新增问题

### 四、已消除问题

### 五、等级变化问题

### 六、仍未解决问题

### 七、结论与下一步建议
```

### `trend <开始日期> <结束日期>`：质量趋势分析

执行步骤：

1. 读取指定时间段内所有日期目录下的 `summary.md`；如不存在，读取 round 报告汇总。
2. 按日期生成 P0/P1/P2/P3、阻断问题、必须修复问题、待验证问题、遗留问题趋势。
3. 识别连续恶化、持续未修复、反复出现、修复后复发的风险。
4. 输出质量趋势判断：改善、恶化、波动、停滞、样本不足。
5. 如果报告缺失，明确列出缺失日期，不要伪造趋势。

输出格式：

```markdown
## 质量趋势分析：YYYY-MM-DD 至 YYYY-MM-DD

### 一、趋势结论

### 二、日期覆盖情况

### 三、核心指标趋势

| 日期 | P0 | P1 | P2 | P3 | 阻断上线 | 必须修复 | 待验证 | 遗留问题 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |

### 四、持续未解决问题

### 五、反复出现或复发问题

### 六、风险模块趋势

### 七、管理建议
```

## 分轮执行方式

### 第 1 轮：项目体检和风险地图

先不要下最终上线结论。输出：

1. 技术栈判断
2. 系统分层结构
3. 系统模块划分
4. 核心业务链路
5. 高风险模块清单
6. 已发现的明显 P0/P1 线索，如有
7. 下一步建议重点审计哪些文件、类、方法

### 第 2 轮：高风险模块专项审计

对合同、支付、审批、租赁、文件上传、权限、定时任务、外部接口、消息消费等高风险模块专项审计。重点检查：

- 事务一致性
- 幂等控制
- 状态流转
- 权限越权
- SQL 注入
- 敏感数据泄露
- 异常处理
- 日志可定位性
- 外部接口超时与降级

输出 P0/P1/P2/P3 问题清单，并引用具体文件和方法。

### 第 3 轮：金融系统专项审计

按金融业务系统上线标准专项检查：

1. 金额字段是否使用 `BigDecimal`
2. 是否存在 `double` / `float` 处理金额
3. `BigDecimal` 比较、精度、舍入方式是否正确
4. 业务状态流转是否有状态机约束
5. 是否存在重复提交风险
6. 关键操作是否有幂等控制
7. 关键业务是否有审计日志
8. 是否存在越权查询、越权操作
9. 数据权限是否只依赖前端
10. 删除、作废、撤回、审批等操作是否有权限和状态校验
11. 批处理、定时任务是否可能重复执行
12. 异常时是否可能造成部分成功、部分失败

只输出真实可能影响生产的风险。

### 第 4 轮：上线阻断判断

站在甲方技术负责人视角，输出正式上线门禁意见：

1. 是否建议上线
2. 是否存在 P0 阻断问题
3. 哪些 P1 问题必须上线前修复
4. 哪些问题可以上线后整改
5. 上线前最小整改清单
6. 如果业务强行上线，需要哪些兜底措施
7. 对供应商的整改要求清单

### 第 5 轮：供应商整改清单

整理为正式、客观、专业的供应商整改清单。每个问题包含：

- 问题描述
- 风险等级
- 涉及模块
- 影响范围
- 整改要求
- 验收标准
- 是否必须上线前完成

## 标准输出格式

### 一、总体结论

一句话说明是否建议上线。可选结论：

- 不建议上线，存在 P0 级阻断问题
- 谨慎上线，必须修复 P1 问题后上线
- 可以上线，但需建立整改清单
- 暂未发现严重阻断问题

### 二、系统技术栈与结构判断

说明主要框架、分层结构、核心模块、高风险模块。

### 三、P0 阻断问题

每个问题按以下格式输出：

- 问题标题
- 问题等级
- 涉及文件
- 涉及类 / 方法
- 涉及代码位置
- 关键代码片段
- 问题说明
- 可能后果
- 修复建议
- 是否阻断上线

### 四、P1 高风险问题

同 P0 格式。

### 五、P2/P3 一般问题

简要列出，避免淹没 P0/P1。

### 六、金融系统专项风险

按金额精度、数据一致性、事务控制、权限控制、审计日志、幂等控制、状态机、敏感数据保护逐项说明。

### 七、上线前整改建议

按优先级输出：

1. 上线前必须修
2. 上线前强烈建议修
3. 上线后一个月内修
4. 长期技术债治理

### 八、最终判断

明确说明：

- 是否可以上线
- 需要修复哪些问题后上线
- 哪些问题纳入后续整改
- 对甲方技术管理的建议

## 本地工具建议

如用户确认可以运行本地命令，可按项目类型选择：

Maven：

```bash
mvn clean test
mvn dependency:tree
mvn clean package -DskipTests
```

SpotBugs：

```bash
mvn spotbugs:check
```

Gradle：

```bash
./gradlew clean test
./gradlew build
./gradlew clean build -x test
```

运行后必须区分：工具扫描发现的问题、源码语义审计确认的问题、仍需人工验证的问题。
