# /apg-daily-review

## 目的

在 {{projectName}} 的工作结束后沉淀经验和上下文变化。

## 步骤

1. 总结修改范围和验证结果。
2. 判断是否发现新的隐藏规则、危险模块或不可信测试。
3. 按 lesson 格式建议更新 `CLAUDE.lessons.md`。
4. 如果分支上下文变化，建议更新 `CLAUDE.local.md`。

## 停止条件

除非用户明确要求，否则不要自动写入 lessons。
