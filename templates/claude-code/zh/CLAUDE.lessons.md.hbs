# CLAUDE.lessons.md

{{projectName}} 的历史经验和 AI 易误改点。

## Lesson 1：不要随意删除 legacy / deprecated 模块

- 问题：Legacy 代码可能仍有隐藏调用方或兼容义务。
- 原因：历史项目里新旧链路经常长期共存。
- 实际背景：{{architecture.legacyPath}}
- 风险：删除或简化兼容代码可能破坏线上用户。
- 规则：除非人工确认，否则把 legacy 模块当作受保护区域。
- 正确做法：先追踪调用方、说明影响面，再请求 review。

## Lesson 2：不要随意重命名共享状态字符串

- 问题：状态字符串可能写入数据库，并被后端、前端、测试、报表共同使用。
- 原因：即使编译通过，历史数据和外部消费者仍可能依赖旧值。
- 实际背景：{{inlineList contracts.statusContracts}}
- 风险：运行时错误、UI 状态错误、迁移失败。
- 规则：变更状态值至少属于 Tier 2，通常需要兼容计划。
- 正确做法：识别所有消费者，先设计迁移和兼容方案。

## Lesson 3：不要局部修改 API 包络或 DTO/VO 结构

- 问题：后端响应结构和前端 API 类型通常强耦合。
- 原因：前端可能依赖固定包络、路径前缀、字段名。
- 实际背景：{{inlineList contracts.apiContracts}}
- 风险：后端测试通过但前端运行失败。
- 规则：任何 API 契约变更至少属于 Tier 2。
- 正确做法：后端、前端类型、API client、测试一起更新。

## Lesson 4：不要把占位测试当作安全证明

- 问题：部分测试可能只是占位或覆盖不足。
- 原因：测试通过不代表真实业务路径安全。
- 实际背景：{{inlineList tests.invalidOrPlaceholderTests}}
- 风险：错误地相信修改安全，导致线上问题。
- 规则：解释测试证明了什么，也说明没有覆盖什么。
- 正确做法：运行真实验证命令，并补充人工/聚焦验证。

## Lesson 5：不要记录或输出敏感值

- 问题：token、签名 URL、access key、private key、模型响应可能包含敏感信息。
- 原因：日志和生成文件可能被复制或提交。
- 实际背景：{{inlineList architecture.externalSystems}}
- 风险：凭证泄漏或隐私事故。
- 规则：只记录字段名/文件名，不输出真实值。
- 正确做法：输出前脱敏，不向用户索要 secret。
